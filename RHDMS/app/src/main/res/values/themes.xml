<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RHDMS" parent='Theme.AppCompat.Light'>
        <!-- Customize your light theme here. -->
        <!-- 원래 값은 parent="Theme.Material3.DayNight.NoActionBar"-->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomArrayAdapterStyle" parent="Theme.AppCompat.Light">
        <!-- Customize your style here -->
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="Theme.RHDMS" parent="Base.Theme.RHDMS" />
</resources>